public class CarConstructor
{
    public string modelName;
    public string modelType;

    public CarConstructor(string model, string type)
    {
        modelName = model;
        modelType = type;
    }
}